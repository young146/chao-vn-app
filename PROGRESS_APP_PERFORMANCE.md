# 앱 전면 점검 — 측정 결과와 개선 계획

> **작성**: 2026-08-05 · **기준 버전**: production rv 2.4.3 (출시빌드 2026-06-25)
> **원칙**: 추측하지 않는다. 모든 숫자는 이 문서에 적힌 방법으로 **실측**한 값이다.
> **이어가기**: 진행 상황은 [WORKLOG.md](WORKLOG.md), 이 문서는 *무엇을 왜 고치는지*의 근거.

---

## 0. 한 줄 요약

앱이 느린 이유는 코드가 나빠서가 아니라 **"앱이 서버가 할 일을 대신 하고, 안 쓸 데이터를 미리 다 받아두기" 때문**이다.
뉴스탭만 예외적으로 서버가 조립해 준다 — 그래서 뉴스탭만 빠르다. 나머지를 그 구조로 옮기는 것이 이 계획의 뼈대다.

---

## 1. 실측 데이터

### 1-1. 앱을 켜면 벌어지는 일 (cold start)

| 순서 | 무슨 일 | 비용 |
|---|---|---|
| 1 | 스플래시 애니메이션 | **무조건 5.0초 고정 대기** ([App.js:531](App.js#L531), 캐시가 있어 0.2초에 준비돼도 5초를 채운다) |
| 2 | 탭 7개를 **전부 동시에** 마운트 (`lazy: false`, [App.js:1642](App.js#L1642)) | 아래 3~5가 한꺼번에 |
| 3 | 당근/나눔: Firestore **500건** ([XinChaoDanggnScreen.js:195](screens/XinChaoDanggnScreen.js#L195)) | 화면엔 20개 남짓 보이는데 500건 |
| 4 | 구인구직: jobs 60 + candidates **100** ([JobsScreen.js:273](screens/JobsScreen.js#L273)) | |
| 5 | 매거진: WordPress **11회 호출** | 카테고리 2회(순차) + 섹션 9회 |
| 6 | 뉴스: WordPress 1회 | **780KB** |
| | **합계** | **Firestore 660건 읽기 + HTTP 12회 + 약 1MB** |

> 부동산·이웃사업은 `useFocusEffect` 라 탭을 눌러야 조회한다(올바름). 당근·구인구직만 `useEffect` 라 부팅 시 실행된다.

### 1-2. 데이터 크기 — "안 읽을 본문"이 대부분이다

| 화면 | 받는 양 | 그 중 기사 본문 | 실제로 화면이 쓰는 것 |
|---|---|---|---|
| 뉴스탭 | **780KB** (기사 95건) | **285KB = 78%** | 제목·썸네일·요약 |
| 매거진탭 (개선 전) | 1,300KB (36건) | 대부분 | 제목·썸네일 |
| 매거진탭 (2026-08-05 개선 후) | **295KB** | 0 | 〃 |

목록에서 본문을 미리 받는 것은 **읽지도 않을 기사 95건의 본문을 매번 내려받는 것**과 같다.
그리고 그게 그대로 AsyncStorage 캐시가 되어, 탭을 열 때마다 읽고 파싱하느라 화면이 멈칫한다.

### 1-3. 응답 시간 (유선 PC 기준 — 베트남 모바일은 2~3배로 보면 된다)

```
뉴스 API (서버가 조립)              0.4 ~ 1.0초   ← 목표 모델
매거진 홈 (앱이 11회 조립)   카테고리 0.95초 + 섹션 2.3초 = 3.3초
```
매거진이 느린 이유는 전송량이 아니라 **호출을 11번 하기 때문**이다(데이터를 4.4배 줄여도 시간은 그대로였다).

### 1-4. 앱에 들어있는 이미지

| 파일 | 크기 | 쓰임 |
|---|---|---|
| `assets/og_jobs_seeker.png` | **1.4MB** | 구인구직 3개 화면의 장식 이미지 |
| `assets/hub-bg.jpg` | **884KB** | 첫 화면(허브) 배경 |
| `assets/og_jobs.png` | 376KB | **코드에서 쓰는 곳 없음(추정)** |
| 합계 | 2.7MB | |

장식 이미지 한 장이 1.4MB 인 것은 원본을 그대로 넣었기 때문이다. 적정 크기는 50~100KB.

### 1-5. 번역 (비한국어 사용자)

`TranslatedText` 는 **컴포넌트 하나당 번역 API 를 1번씩** 부른다([TranslatedText.js:14](components/TranslatedText.js#L14)).
뉴스탭 한 화면 = 제목 약 80개 = **API 80회**. 묶어서 부르는 함수(`translateTexts`)가 이미 있는데 아무도 안 쓴다.
(캐시는 있어서 두 번째부터는 안 부르지만, 새 기사는 매일 바뀐다 = 매일 80회.)

### 1-6. 측정 인프라 — 지금 iOS 사용자는 보이지 않는다 🔴

- `GoogleService-Info.plist` 의 `IS_ANALYTICS_ENABLED` = **true** ✅
- `app.json` 의 `FIREBASE_ANALYTICS_COLLECTION_ENABLED` = **true** ✅
- 그런데 **마지막 출시 빌드는 2026-06-25**, 이 설정 커밋은 **2026-07-03**.
- → **설정은 고쳐져 있지만 빌드에 안 들어갔다. iOS 측정은 지금도 0건.** OTA 로는 해결 불가(네이티브 설정).

---

## 2. 개선 계획 — 효과·위험·비용

### Phase 1 — 순수 JS, OTA 1회로 끝 (즉시 체감)

| # | 할 일 | 기대 효과 | 상태 |
|---|---|---|---|
| 1-A | **매거진 탭 수정** — 실패 캐시 제거·즉시표시·데이터 27배 축소 | 빈 화면 증상 소멸 | ✅ 완료 `d0507d7` |
| 1-B | **스플래시 5.0초 → 2.0초** (`SPLASH_MIN_MS`) | 앱 켤 때마다 **3초** 단축 | ✅ 완료 |
| 1-C | **당근·구인구직의 부팅 시 조회 제거** (탭을 실제로 열 때만 조회) | 부팅 시 Firestore **660건 → 0건** | ✅ 완료 |
| 1-E | 이웃사업 FlatList 최적화 옵션 보강 | 스크롤 버벅임 감소 | ✅ 완료 |

**1-C 는 계획을 바꿨다 (중요).** 원래는 "당근 첫 로딩 500 → 60" 이었는데, 조사해 보니
당근 화면은 **카테고리·도시·구·아파트 필터를 그 500건 위에서 클라이언트가** 건다
([XinChaoDanggnScreen.js:267](screens/XinChaoDanggnScreen.js#L267)). 60 으로 줄이면 필터 결과가 텅 빈다.
→ 건수는 그대로 두고 **"부팅할 때 읽던 것"만** 없앴다. 효과는 오히려 더 크다(660건 → 0건).
건수 자체를 줄이는 것은 **서버 쪽 필터링(Firestore where + 인덱스)** 이 전제라 Phase 4 로 옮긴다.

**1-D(candidates 100 → 30)는 하지 않았다.** 그 쿼리에는 `orderBy` 가 없어서
([JobsScreen.js:278](screens/JobsScreen.js#L278)) 100건이 "최신 100건"이 아니라 **임의의 100건**이다.
숫자만 줄이면 최신 구직글이 목록에서 빠질 수 있다. 1-C 로 부팅 부하는 이미 사라졌으므로,
정렬을 붙이는 일(=`createdAt` 이 없는 문서가 통째로 사라질 위험 검토 포함)은 Phase 4 에서 데이터를 보고 한다.

**덤으로 고쳐진 것**: 구인구직은 예전에 마운트 때 딱 한 번만 읽어서, 공고를 새로 올리고
목록으로 돌아와도 **방금 올린 글이 안 보였다**(앱을 껐다 켜야 보임). 이제 목록에 돌아올 때 다시 읽는다.

### Phase 2 — 서버가 무거운 일을 하게 (플러그인 수정 + FTP)

| # | 할 일 | 기대 효과 | 위험 |
|---|---|---|---|
| 2-A | **뉴스 API 에서 본문 제거** + 상세화면이 본문만 따로 조회 | 뉴스탭 **780KB → 약 170KB** (78% 감소) | 중 (상세화면 경로 변경 — 매거진에서 이미 검증한 방식) |
| 2-B | **매거진 홈 조립 API 신설** (뉴스터미널과 같은 방식) | 11회 → **1회**, 3.3초 → **1초 이하** 예상 | 중 (새 엔드포인트, 앱은 폴백 유지) |

### Phase 3 — EAS Build + 스토어 제출 필요

| # | 할 일 | 기대 효과 | 위험 |
|---|---|---|---|
| 3-A | 🔴 **iOS Firebase Analytics 살리기** | iOS 사용자 행동이 **처음으로** 보이기 시작 | 없음(설정은 이미 커밋됨, 빌드만 하면 됨) |
| 3-B | 이미지 자산 최적화 2.7MB → 약 0.3MB | 앱 설치 용량·첫 화면 메모리 | 낮음 |
| 3-C | iOS 푸시 알림 이미지(NSE) | 푸시 열람률 | 중 |

### Phase 4 — 구조 개선 (Phase 1~3 효과 확인 후)

| # | 할 일 |
|---|---|
| 4-A | 번역을 화면 단위로 묶어서 호출 (80회 → 1~2회) |
| 4-B | `lazy: false` 재검토 — 무거운 탭만 지연 로딩 |
| 4-C | 화면별 제각각인 캐시 로직을 공통 규칙으로 통일 (지금은 5개 화면이 각자 다르게 구현) |

---

## 3. 진행 기록

| 날짜 | Phase | 내용 | 상태 |
|---|---|---|---|
| 2026-08-05 | — | 전면 감사 + 이 문서 작성 | ✅ |
| 2026-08-05 | 1-A | 매거진 탭 수정 (`d0507d7`) | 코드 완료 |
| 2026-08-05 | 1-B/C/E | 스플래시 2초 · 부팅 조회 제거 · 목록 최적화 | 코드 완료 |
| | | **→ Phase 1 전체를 OTA 1회로 발송 예정** | 승인 대기 |

### 사장님 결정 (2026-08-05)
- 스플래시: **2초** 로 단축
- EAS Build(+스토어 제출): **Phase 1·2 를 끝내고 모아서** 한 번에 — 그때 iOS 측정·이미지 최적화·푸시 이미지를 함께 넣는다
