# 매거진 콘텐츠 파이프라인 — 진단과 개선

> **작성**: 2026-08-06 · **범위**: 격주 발행 잡지 → 워드프레스 → 웹/앱 노출까지의 전 과정
> **관련**: [PROGRESS_APP_PERFORMANCE.md](PROGRESS_APP_PERFORMANCE.md)(속도), [WORKLOG.md](WORKLOG.md)(진행)

---

## 1. 문제 (사장님 제기)

> "잡지는 매호마다 필요에 의해 새로운 콘텐츠가 나오는데, 지금 구조(섹션이 고정)에서는 새 콘텐츠가 반영이 잘 안 된다."

측정해 보니 **짐작보다 심각했다.**

### 1-1. 앱은 잡지의 16%를 아예 못 보여주고 있었다

최근 매거진 글 100건을 앱의 고정 9섹션 기준으로 대조:

| | 건수 |
|---|---|
| 앱 섹션에 속함 | 84건 |
| **어느 섹션에도 안 속해 앱에서 볼 수 없음** | **16건 (16%)** |

안 보이던 것이 하필 잡지의 간판 꼭지였다:

| 카테고리 | 건수 | 왜 안 보였나 |
|---|---|---|
| BUSINESS · INTERVIEW | 12건 | 부모가 `피플(14)` — 앱 9섹션에 없음 |
| DESK TALK | 5건 | 최상위 카테고리인데 앱 목록에 없음 (웹 메뉴엔 맨 위) |
| EVENT · MOVIE | 4건 | 부모가 `문화 & 교양(9)` |
| REAL ESTATE · VN Information | 2건 | 부모가 `VN Information(4)` |

전체 카테고리 **138개 중 앱이 보는 것은 89개**. 49개가 사각지대였다.
게다가 최근 글에 `Lifestyle Trend(747)` 처럼 **새로 만든 카테고리**가 등장한다 —
고정 목록 방식에서는 **개발자가 코드를 고치기 전까지 영원히 안 보인다.**

### 1-2. "몇 월호"라는 개념이 데이터에 없다

- 태그가 달린 글: **0건 / 100건**
- 즉 어떤 글이 어느 호에 실린 것인지 시스템이 모른다 → **"이번 호 목차"를 만들 수 없다.**
- 그래서 앱·웹은 "카테고리별 최신 4건"이라는 우회 표현만 하고 있다. 잡지인데 잡지처럼 안 보이는 이유.

### 1-3. 다행인 것 — 업로드 리듬은 매우 규칙적이다

```
6/1-2   17건        6/15-16 20건        6/29-30 19건        7/13 14건        7/27-28 20건
        └── 정확히 2주 간격, 한 호 = 17~20건, 이틀에 걸쳐 업로드 ──┘
```
직원들이 **워드프레스 관리자에서 직접 작성**한다(2026-08-06 확인).

---

## 2. 개선 방향

### A. 사각지대 0 — 섹션 자동 발견 (서버만 수정) ✅ 구현완료

고정 9섹션은 그대로 두고(이름·순서는 사람이 정한 것이라 가치가 있다),
**거기 안 잡히는 최근 글을 찾아 자동으로 섹션을 붙인다.**

규칙:
1. **데일리 뉴스(31)와 그 하위는 제외** — 매거진이 아니다.
   ⚠️ 31 은 `뉴스(6)` 밑에 있어서, 루트째로 묶으면 매일 뉴스가 매거진 탭에 섞인다. 반드시 걸러야 한다.
2. 최근 6개월 글 중 기존 섹션에 안 잡히는 것을 찾아, **"아직 커버 안 된 가장 위쪽 조상"** 으로 묶는다.
   → BUSINESS·INTERVIEW 가 각각이 아니라 `피플` 하나로 묶인다.
3. 2건 미만은 섹션으로 만들지 않는다(일회성 카테고리 방지).
4. 관리자가 특정 섹션을 숨기려면 옵션 `chaovn_magazine_hidden_sections` 에 ID 를 넣는다.

**사전 시뮬레이션 결과** — 새로 생길 섹션:

| 섹션 | 최근 글 |
|---|---|
| 피플 | 12건 |
| DESK TALK | 5건 |
| 문화 & 교양 | 4건 |
| VN Information | 2건 |

→ 기존 9 + 새 4 = **사각지대 0**.
**앱 배포가 필요 없다** — 앱은 서버가 주는 섹션 목록을 그대로 그리므로, 플러그인만 올리면 반영된다.

### B. "이번 호" 축 도입 — 잡지를 잡지답게 ✅ 구현완료 (FTP+OTA 대기)

글마다 **발행호**를 붙이면, 매호 새 꼭지가 나와도 자동 반영되고 "지난 호 아카이브"가 공짜로 생긴다.

**직원 작업을 늘리지 않는 설계**가 핵심이다:

1. `magazine_issue` 라는 분류를 새로 만든다(카테고리처럼 관리자 화면에 체크박스로 표시).
2. 관리자 설정에서 **"이번 호"를 한 번만 지정**한다 (예: `2026년 8월 A호`).
3. 글을 발행하면 **호가 자동으로 붙는다** — 직원은 아무것도 안 해도 된다.
   (뉴스(31)는 제외. 이미 호가 붙어 있으면 건드리지 않는다.)
4. 과거 글은 발행일 클러스터로 **일괄 부여**(1회성 도구). 위 1-3 의 규칙적 리듬 덕에 정확도가 높다.
5. 앱·웹에 **"이번 호"** 화면: 표지 + 그 호의 실제 목차(그 호가 가진 꼭지대로).

→ 새 꼭지가 생겨도 코드 수정 영원히 불필요. 이것이 근본 해결이다.

**호 번호 체계 (2026-08-06 사장님 확인)**: 564호 = 7/27, 565호 = 8/9.
한 달에 두 번이지만 **항상 격주는 아니다 — 3개월에 한 번은 3주 텀**이 생긴다.
→ **날짜로 호를 계산하지 않는다.** 계산식은 반드시 어긋난다.
   "지금 몇 호인가"는 사람이 한 번 지정하고, 시스템은 그것만 따른다.

**과거 글 일괄 부여**: 업로드 날짜 뭉치로 묶는다(3일 이상 비면 다른 호).
발행 주기는 불규칙해도 **업로드는 발행 즈음 하루이틀에 몰린다**(실측)는 점을 이용한 것이다.
관리 화면에서 미리보기 → 확인 후 실행. 이미 호가 지정된 글은 덮어쓰지 않는다.

**표지**: 호의 속성으로 둔다(글이 아니라). 워드프레스 미디어 라이브러리에서 호당 1장 선택.
issuu 에 올리는 표지 파일을 그대로 쓰면 되므로 새로 만들 필요가 없다.
표지를 안 올린 호도 화면이 깨지지 않게 **앱이 대체 표지를 그린다**(호수 텍스트 카드).
> 완벽한 운영을 전제하는 시스템은 언젠가 반드시 깨진다. 빠져도 굴러가게 만든다.

### D. PDF(issuu) 공개 시점 — 발행 1~2주 후로 변경 (2026-08-06 결정)

사장님 관찰: *"issuu 로 PDF 를 무료로 보여줬는데 그게 오히려 독이 되어 직원이 콘텐츠를 제대로 안 올렸다."*

이건 직원 문제가 아니라 **구조가 만든 결과**다:

| 입장 | 지금 구조 |
|---|---|
| 독자 | PDF 에 다 있는데 기사를 클릭할 이유가 없다 → 웹·앱 체류 0 |
| 직원 | PDF 올렸으면 일이 끝난 것 → 기사 업로드는 "추가 잡무" |
| 회사 | 검색 유입 0(PDF 속 글자는 색인 안 되거나 issuu 도메인이 가져감) · 광고 지면 0 · 데이터 0 |

**결정**: 최신호는 웹/앱 기사로 먼저, PDF 전문은 1~2주 후 공개. 지난 호 아카이브는 그대로 공개.
→ 독자를 우리 채널로 돌리고, **직원에게는 "기사를 안 올리면 최신호가 비어 보인다"는 구조**가 생긴다.
지시나 독려보다 구조가 강하다.

### C. 업로드 품질 — 발행 체크리스트 (B 와 함께)

지금은 글마다 사람이 카테고리를 판단한다 → 새 꼭지엔 새 카테고리를 만들고 → 노출 설정과 어긋난다.
A 로 "어긋나도 보이게" 만들고, B 로 "호 단위로 묶이게" 만든 뒤, 남는 것은 **일관성**이다.

관리자 화면에 발행 체크리스트를 붙인다(대표이미지 없음·요약 없음·호 미지정 글을 발행 전에 잡아준다).

---

## 3. 진행 기록

| 날짜 | 항목 | 상태 |
|---|---|---|
| 2026-08-06 | 진단 (16% 사각지대, 태그 0건, 업로드 리듬 측정) | ✅ |
| 2026-08-06 | A. 섹션 자동 발견 | ✅ **배포완료** — 섹션 9 → 13 (피플·DESK TALK·문화&교양·VN Information 등장). 앱 배포 없이 서버만으로 반영 |
| 2026-08-06 | B. 호 축 도입 | ✅ **배포완료** — 플러그인 FTP + 앱 OTA(group `d4693711`) |
| 2026-08-06 | 이번 호 기사 화면 | ✅ 배포완료 (OTA `c14c3622`) — 꼭지별 자동 구성 |
| 2026-08-06 | 지난 호 보기 (표지 격자) | ✅ 배포완료 (OTA `a5e3fcd3`) |
| | C. 발행 체크리스트 | 미착수 (다음 호 운영 보고 판단) |

### 표지 운영 규격 (담당자 안내용)

| 항목 | 권장 |
|---|---|
| 폭 | 1000px 내외 (앱 최대 사용처 480px 의 2배) |
| 비율 | **잡지 판형 그대로 — 자르지 말 것** |
| 형식·용량 | JPG · 400KB 이하 |

비율을 규격으로 강제하지 않는 이유: 서버가 표지의 실제 가로·세로(`coverWidth/coverHeight`)를
함께 보내고 앱이 그 비율대로 칸을 그린다. A4든 국배판이든 잘리지도 여백이 생기지도 않는다.
**"몇 대 몇으로 잘라 오세요"를 요구하는 시스템은 언젠가 반드시 어긋난다.**

### 화면 구성 (2026-08-06 기준)

```
매거진 탭
 └ 📖 이번 호  [발행 예정]              지난 호 ›
   ├ 표지(실제 비율, 화면폭 34%) ← 누르면 이번 호 기사
   ├ 제565호 · 2026.08.09 발행 예정
   └ [이번 호 기사 전체 보기 ›]

 이번 호 기사 화면 : 표지 + 꼭지별 목록(DESK TALK / BUSINESS / …)
 지난 호 화면      : 표지 3열 격자 → 누르면 그 호 기사
```

기사가 올라오면 **사람이 목차를 만들 필요 없이** 두 화면이 저절로 채워진다.

### B 배포 후 실측 (2026-08-06)

| | |
|---|---|
| 호가 붙은 글 | **397건 / 19개 호** (제546호 ~ 제564호) |
| 적용 구간 | 2025-11-19 ~ 2026-07-28 |
| 이번 호 | 제565호 · 2026-08-09 · 수록 0건 (발행 전이라 정상) |
| 슬러그 | 전부 `issue-546` ~ `issue-565` (한글 인코딩 0개) |
| 검증 | 10개 뭉치 전수 대조 일치 · 호 2개 붙은 글 0건 |

**과거분을 여기까지만 붙인 이유** (사장님과 합의):
매거진 글은 전체 7,090건인데, 경계 바로 앞 **2025-11-18 하루에 171건**이 몰려 있다.
잡지 한 호(13~24건)일 수 없는 양 — 사이트 이전이나 과거분 일괄 업로드의 흔적이다.
그 앞으로 더 붙이면 171건이 통째로 한 호가 되어 **그 아래 번호가 전부 밀린다.**
없는 것보다 틀린 것이 나쁘므로 **최근 19개 호만 정확히** 붙이고, 그 이전은 날짜·검색으로 찾는다.
→ 재발 방지로 뭉치 상한선(40건)을 코드에 박아 뒀다.

---

# 4. 운영 매뉴얼 — 매호 무엇을 하나 (직원 교육용)

> **작성 2026-08-25.** 사장님 요청: *"이번호/지난호 기사 노출과 PDF 판보기까지, 직원 교육용으로 정리."*
> 이 장(章)만 따로 열어 화면에 띄우고 교육해도 되도록 **자체 완결**로 썼다.
> 근거는 전부 소스 실물: `wp-plugins/chaovn-news-api/chaovn-news-api.php`(v2.3.2) · `screens/MagazineScreen.js` ·
> `screens/IssueContentsScreen.js` · `screens/IssueArchiveScreen.js`.

## 4-0. 한 줄 요약

**모든 것은 워드프레스의 「매거진 호」 하나에서 나온다.**
거기에 호수·발행일·표지·PDF링크만 넣으면 **웹과 앱이 같은 데이터로 저절로 채워진다.**

> ❗ **앱에는 아무것도 "올리지" 않는다.** 앱은 서버가 주는 것을 그대로 그린다.
> 새 호가 나와도 **앱 배포(빌드·OTA)는 필요 없다.** — 사장님이 "앱은 어떻게 올렸더라?" 가 기억 안 나는 이유가 이것이다. 올린 적이 없다.

## 4-1. 직원이 하는 일 — 딱 3가지 + 평소 기사쓰기

| # | 언제 | 어디서 | 무엇을 |
|---|---|---|---|
| 1 | 새 호 나오기 전 | `글 → 매거진 호 → 새 호 추가` | **호 이름**(예: 제566호) · **호수**(566) · **발행일** · **표지 이미지** |
| 2 | 새 호 기사 올리기 시작할 때 | `글 → 매거진 호 설정` → "1. 편집 중인 호 지정" | 방금 만든 호를 고르고 **저장** |
| 3 | 발행 **1~2주 후** | `글 → 매거진 호 → 그 호 편집` | **PDF 보기 링크** 칸에 issuu·구글드라이브 주소 붙여넣기 |
| — | 평소 | 글쓰기 | **그냥 쓰면 된다.** 발행하면 2번에서 지정한 호에 **자동으로** 들어간다 |

> ⚠️ 교육에서 반드시 강조: **기사 쓸 때 "매거진 호" 체크박스를 손대지 말 것.** 자동이다. 손대면 오히려 어긋난다.
> (예외: 잡지 기사가 아닌 상시 콘텐츠 — 교민소식 등 — 은 체크를 **해제**한다. 4-6 참조)

**표지 규격**: 폭 1000px 내외 · **잡지 판형 그대로, 자르지 말 것** · JPG 400KB 이하.
서버가 표지의 실제 가로·세로(`coverWidth/coverHeight`)를 함께 보내고 앱이 그 비율대로 칸을 그리므로
**"몇 대 몇으로 잘라 오세요"가 없다.** 자르면 오히려 어긋난다.

## 4-2. "이번 호"가 넘어가는 방식 — 사람이 안 해도 된다

가장 헷갈리는 지점. **호는 두 가지 뜻으로 쓰인다.**

| | 뜻 | 누가 정하나 |
|---|---|---|
| **편집 중인 호** | 지금 올리는 기사가 들어갈 호 | 사람이 지정 (4-1 의 ②) |
| **독자에게 보이는 "이번 호"** | 웹·앱 맨 위에 뜨는 호 | **자동** — *발행일이 지난 호 중 가장 최신* (`chaovn_get_display_issue_id`) |

즉 8/25 에 566호(발행일 9/6)를 미리 만들어 편집 대상으로 걸어 두어도,
**독자에게는 9/6 이 되기 전까지 565호가 보인다.** 발행일이 되면 **사람 손 없이 저절로** 넘어간다.
관리 화면에 *"지금 독자에게 이번 호로 보이는 것"* 이 함께 표시된다 — **두 값이 달라도 정상**이다.

## 4-3. 노출되는 자리 — 웹

| 자리 | 주소 / 방법 | 무엇이 뜨나 | PDF 버튼 |
|---|---|---|---|
| **매거진 페이지** | `/magazine_flip/` (숏코드 `[chaovn_magazine]`) | 이번 호 히어로(표지·호수·발행일·꼭지 8개) + **지난 호 표지 격자 14개** | 지난 호 카드에만 ✅ |
| **호 페이지** | `/magazine-issue/issue-566/` (자동 생성) | 그 호 표지 + 꼭지별 전체 목차 | ✅ (링크 넣은 호는 이번 호도 표시) |
| **사이드바 위젯** | 사장님이 직접 만든 **이미지 위젯** → `/magazine_flip/` 링크 | 표지 이미지 | 없음 |

> 🔧 **사이드바 위젯은 개선 여지가 있다.** 지금은 표지를 **손으로 올린 이미지**라 **호가 바뀔 때마다 갈아 끼워야 하고,
> 격주 발행이면 언젠가 반드시 잊는다.** 이걸 위해 만들어 둔 것이 `[chaovn_issue_cover]` 숏코드다 —
> 텍스트 위젯에 `[chaovn_issue_cover width="200"]` 한 줄이면 **이번 호 표지가 자동 갱신**된다.
> (2026-08-07 에 만들었으나 결과적으로 미사용 상태. 텍스트 위젯의 숏코드 실행은 **실물 확인 전**)

**PDF 정책** (2026-08-06 사장님 결정): **최신호는 PDF 를 비워 둔다.**
PDF 가 먼저 공개되면 ①독자가 기사를 클릭할 이유가 없어 웹·앱 체류가 0 이 되고 ②직원에겐 "PDF 올렸으면 끝"이 되어
기사 업로드가 잡무가 되며 ③검색 유입·광고 지면·데이터가 전부 0 이 된다.
**발행 1~2주 후 채우면** 그 시점부터 버튼이 **자동으로** 생긴다. (§2-D)

## 4-4. 노출되는 자리 — 앱 (**따로 올릴 것 없음**)

앱은 서버 API 3개를 읽어 그린다. 새 호가 생겨도 **앱 배포·OTA 전혀 불필요.**

```
매거진 탭
 └ 📖 이번 호   [발행 예정]              지난 호 ›
    ├ 표지(실제 비율) ← 누르면 이번 호 기사
    ├ 제565호 · 2026.08.09 발행
    └ [이번 호 기사 전체 보기 ›]  + 가로 스크롤 기사 카드
       ├→ 「이번 호 기사」 화면 : 표지 + 꼭지별 목차 (자동 구성)
       └→ 「지난 호」 화면     : 표지 3열 격자 → 누르면 그 호 기사
```

| 화면 | 파일 | 읽는 곳 |
|---|---|---|
| 이번 호 블록 | `screens/MagazineScreen.js` (`CurrentIssueBlock`) | `/wp-json/chaovn/v1/magazine-home` |
| 이번 호 기사 | `screens/IssueContentsScreen.js` | `/wp-json/chaovn/v1/magazine-issue` |
| 지난 호 격자 | `screens/IssueArchiveScreen.js` | `/wp-json/chaovn/v1/magazine-issues` |

표지를 안 올린 호도 화면이 안 깨진다 — 앱이 **호수 텍스트 카드**를 대신 그린다.

## 4-5. ⚠️ 알려진 구멍 — **앱에는 PDF 버튼이 없다**

서버는 `pdfUrl` 을 앱에도 **보내고 있는데**, 앱 화면 어디에서도 그 값을 쓰지 않는다
(앱 소스 전체에 `pdfUrl` 사용처 **0곳**, 2026-08-25 확인). 즉 **PDF 판보기는 현재 웹 전용.**

- **원인**: 8/6 에 앱 화면 3종을 먼저 만들고, PDF 링크 필드는 그 다음날 8/7 **웹 작업 중** 신설됐다.
  서버 응답에는 공용 함수(`chaovn_get_issue_payload`) 덕에 자동으로 실렸지만, **앱 화면에 버튼을 붙이는 작업이 빠졌다.**
- **고치는 비용**: 순수 JS → **OTA 한 번.** 빌드·심사 불필요.
- **상태**: 미착수 (사장님 지시 대기)

## 4-6. 매호 체크리스트 (이대로 교육)

```
□ ① 발행 며칠 전  : 글 → 매거진 호 → 새 호 추가
                    호 이름 "제566호" / 호수 566 / 발행일 / 표지(자르지 말 것)
□ ② 기사 올리기 전 : 글 → 매거진 호 설정 → "편집 중인 호"를 566호로 저장
□ ③ 기사 업로드    : 평소대로. 호는 자동으로 붙음 (손대지 말 것)
                    대표이미지·카테고리는 꼭 지정
□ ④ 발행일 당일    : 아무것도 안 해도 웹·앱의 "이번 호"가 저절로 566호로 바뀜
                    (안 바뀌면 → 호 편집에서 발행일 입력 확인)
□ ⑤ 발행 1~2주 후  : 글 → 매거진 호 → 566호 편집 → PDF 보기 링크 입력
                    → 웹 지난호 카드·호 페이지에 "PDF로 보기" 버튼 자동 생성
```

**직원이 흔히 낼 사고 3가지**

| 증상 | 진짜 원인 | 조치 |
|---|---|---|
| 새 호가 웹·앱에 안 뜬다 | 발행일 미입력 또는 미래 날짜 | 호 편집에서 발행일 확인 |
| 상시 콘텐츠가 잡지 호에 섞인다 | "편집 중인 호"가 걸린 채로 교민소식 등을 발행 | 잡지 기사가 아니면 글 편집화면에서 **호 체크 해제** |
| 표지가 잘려 보인다 | 표지를 임의 비율로 잘라 올림 | **판형 그대로** 재업로드 |
