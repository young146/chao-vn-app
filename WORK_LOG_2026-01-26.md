# 작업 로그 - 2026년 1월 26일

## 📋 오늘 완료한 작업

---

### 1️⃣ 태그 기반 위치별 광고 시스템

**파일**: `components/AdBanner.js`

**기능**:
- WordPress 태그를 사용하여 광고 위치별 세분화
- 각 화면별로 다른 광고 표시 가능
- 광고 가격 차등화 지원

**WordPress 태그 목록**:
| 태그 | 위치 |
|------|------|
| `home_header` | 홈 화면 최상단 |
| `home_section` | 홈 화면 섹션 사이 |
| `news_header` | 뉴스 화면 최상단 |
| `news_inline` | 뉴스 기사 사이 |
| `board_header` | 게시판 최상단 |
| `board_inline` | 게시판 글 사이 |
| `nanum_header` | 나눔 화면 최상단 |
| `item_detail` | 아이템 상세 화면 |

**작동 방식**:
1. 태그가 있는 광고 → 해당 위치에만 표시
2. 태그가 없는 광고 → 모든 위치에서 랜덤 표시
3. ChaoVN Ad API 우선, 실패 시 WordPress Posts API 사용

---

### 2️⃣ Firebase 초기화 속도 개선

**파일**: `App.js`

**변경 사항**:
- Firebase 초기화 타임아웃: 5초 → 2초
- 순차 초기화 → 병렬 초기화 (Promise.allSettled)
- 폴링 간격: 100ms → 50ms

**결과**:
- 이전: 5~7초
- 이후: **38ms** (99% 개선!)

---

### 3️⃣ Universal Link / App Link 설정

**목적**: SNS 링크 클릭 시 웹사이트 거치지 않고 바로 앱 열기

**파일**: `app.json`

**iOS (Universal Link)**:
```json
"associatedDomains": [
  "applinks:chaovietnam.co.kr",
  "applinks:www.chaovietnam.co.kr"
]
```

**Android (App Link)**:
```json
"intentFilters": [
  {
    "action": "VIEW",
    "autoVerify": true,
    "data": [
      { "scheme": "https", "host": "chaovietnam.co.kr", "pathPrefix": "/" },
      { "scheme": "https", "host": "www.chaovietnam.co.kr", "pathPrefix": "/" }
    ],
    "category": ["BROWSABLE", "DEFAULT"]
  }
]
```

**웹서버 설정** (이미 완료):
- `/.well-known/apple-app-site-association` (iOS)
- `/.well-known/assetlinks.json` (Android)

---

### 4️⃣ 스플래시 화면 + 프로그레스 바

**파일**: `App.js`

**변경 사항**:
- `expo-splash-screen` 패키지 추가
- 네이티브 스플래시 → JS 로딩 화면 전환 시 검은 화면 방지
- 프로그레스 바가 있는 로딩 화면 표시

**작동 방식**:
1. 앱 시작 → 네이티브 스플래시 100ms 유지
2. JS 로딩 화면 표시 (아이콘 + 프로그레스 바 + 퍼센트)
3. 초기화 완료 → 메인 화면

---

### 5️⃣ Expo Go 호환성 수정

**파일**: `App.js`

**문제**: `expo-tracking-transparency`가 Expo Go에서 에러 발생

**해결**:
- 동적 import로 변경
- Expo Go 감지 시 건너뜀
- 프로덕션 빌드에서만 ATT 권한 요청

---

## 📦 빌드 정보

| 플랫폼 | 버전 | 빌드 번호 | 상태 |
|--------|------|----------|------|
| iOS | 2.2.0 | 35 | ✅ App Store Connect 업로드 완료 |
| Android | 2.2.0 | 54 | ✅ Play Console 수동 업로드 완료 |

---

## ⚠️ 알려진 문제

### Android 자동 제출 실패
- **원인**: Google Play Service Account 인증 만료
- **에러**: `"invalid_grant","error_description":"Invalid grant: account not found"`
- **해결 필요**: Google Cloud Console에서 새 Service Account 생성 후 EAS에 등록

---

## 📝 다음 작업 예정

1. [ ] Android 자동 제출 설정 (Service Account 갱신)
2. [ ] 링크 연결 속도 테스트 (Universal Link/App Link 적용 후)
3. [ ] 광고 태그 시스템 WordPress에서 테스트

---

## 🔧 Git 커밋 히스토리

```
8a477f6 - perf: parallel Firebase init (5s to 38ms) + fix Expo Go compatibility
47fd3d0 - chore: add expo-tracking-transparency dependency
4246e4f - feat: tag-based position-specific ads (home_header, news_inline, etc)
```

---

## 📂 수정된 파일 목록

- `App.js` - Firebase 병렬 초기화, 스플래시 화면, ATT 호환성
- `app.json` - Universal Link, App Link 설정
- `components/AdBanner.js` - 태그 기반 위치별 광고
- `package.json` / `package-lock.json` - expo-splash-screen 추가

