# 광고 슬롯 재설계 (통합 광고센터)

> **상태**: 🟢 표준 확정 · 구현 완료 — **배포 대기**(WP FTP · 앱 OTA). §10 진행 현황 · §12 다음 단계
> **시작**: 2026-08-23 · **관련**: [WORKLOG.md](WORKLOG.md) · [PROGRESS_REVENUE_MASTERPLAN.md](PROGRESS_REVENUE_MASTERPLAN.md)
>
> 왜 이 문서가 필요한가: 광고를 **등록하는 화면(통합 광고센터)** 과 **실제로 광고가 박히는 자리** 가 어긋나 있다.
> 팔 수 없는 자리를 팔 수 있는 것처럼 보여주고, 페이지를 지정해도 지켜지지 않는다. 둘을 1:1로 맞추는 작업이다.

---

## 🚀 새 대화는 여기서 시작 (2026-08-23 인수인계)

**이 문서 하나만 읽으면 이어서 일할 수 있다.** 앞 대화 내용을 다시 물을 필요 없다.

**첫 행동**: §10 진행 현황과 §12 다음 단계를 읽고, 아직 안 끝난 것부터 이어간다. 슬롯 정본은 §8.

**작업 대상 저장소 (전부 `github.com/young146/<repo>`)**

| 무엇 | 저장소 | 핵심 파일 |
|---|---|---|
| 등록 콘솔 | `daily-news-final` | `app/admin/ad-center/page.js` |
| chaovietnam WP 표시 (홈·상세) | `daily-news-final` | `wordpress-plugin/xinchao-unified-ads.php` |
| 뉴스터미널 표시 (독립 지면) | `daily-news-final` | `wordpress-plugin/jenny-daily-news.php` |
| vnkorlife 표시 | `vnkorlife-web` | 슬롯 컴포넌트 |
| 앱 표시 | `chao-vn-app` | `services/FirebaseAdService.js` · `components/DetailAdBanner` · `screens/PostDetailScreen` |

**먼저 알고 있어야 할 함정 3가지 (실측으로 확인된 것. 다시 검증하지 말 것)**

1. **`.e3lan` 클래스는 광고차단 필터에 등재돼 있다** (Sahifa 테마 광고 영역, 아랍어 '광고'). 여기에 유료 광고를 걸면
   광고차단 쓰는 독자에게 **안 보인다.** → 새 슬롯의 클래스명에 `ad`·`banner`·`e3lan` 을 쓰지 말 것.
2. **이 워드프레스 서버는 외부 HTTP 아웃바운드(`wp_remote_get`)가 막혀 있다.** 플러그인이 외부 API 를 부르는 설계는
   서버에서 조용히 실패한다 → 내장 폴백이 필요하다.
3. **헤드리스 Chrome 진단 시 자동화 플래그를 쓰면 구글이 봇으로 보고 광고를 204(빈 응답)로 준다.**
   슬롯 존재 확인(`--dump-dom`)에는 써도 되지만, **광고가 채워지는지 판단하는 용도로는 쓸 수 없다.**

**앱 쪽 배포 판정**: 뉴스 상세 3자리 추가는 기존 `DetailAdBanner` 재사용 → 네이티브 변경 없음 → **OTA 로 배포 가능**
(`eas update --channel production`). ⛔ `--branch main` / `--channel main` 금지.

---

## 1. 지금 실제로 있는 슬롯 (2026-08-23 라이브 실측)

라이브 HTML 에서 슬롯 호출을 직접 뽑아 확인한 것이다. 문서를 베낀 게 아니다.

### chaovietnam.co.kr
| 페이지 | 실제 슬롯 |
|---|---|
| 시작 페이지(홈) | `sidebar` **1개뿐** (page 지정 없음) |
| 뉴스 터미널 | `news-terminal` / `in-content` **1개뿐** |
| 상세 페이지 | `detail`/`top` 1 · `detail`/`in-content` **6** · `detail`/`bottom` 1 · `sidebar` |

- 본문 자동삽입은 `xinchao_inject_body_ads()` 가 `the_content` 필터로 처리하며 **`is_singular('post')` 조건**이다 → 홈·터미널은 대상이 아니다.
- 중간 광고는 `XINCHAO_BODY_EVERY = 2` 로 **매 2단락마다** 들어가 최대 6칸이 잡힌다.

### vnkorlife.com
| 페이지 | 실제 슬롯 |
|---|---|
| 홈 (`home`) | `top` · `bottom` |
| 목록 (`market`/`jobs`/`realestate`/`neighborbusiness`) | `top` · `in-content`(반복) · `bottom` · `sidebar` |
| 상세 (`*-detail` 4종) | `top` · `in-content` · `bottom` · `sidebar` |
| 블로그 (`blog`) | `in-content` · `sidebar` |

- 위치 타입은 `in-content-1` ~ `in-content-6` 까지 있고, 등록 시 `in-content` 로 넣으면 `startsWith` 매칭으로 전부 잡힌다.
- ⚠️ **옐로페이지는 `neighborbusiness` 가 아니다** (2026-08-23 소스 재확인, 앞 기록 정정).
  - **옐로페이지** = `/yellowpage` (+ 상세 `/biz/[id]`) — 업소 디렉토리 3,700여 곳. 작업 전엔 **광고 슬롯이 하나도 없었다.**
  - **동네업소** = `/neighborbusiness` (+ `/[id]`) — 사용자가 등록하는 업소. 슬롯 4개가 있는데 콘솔에서 지정을 못 했다.
- **블로그 목록(`/blog`)에도 광고가 하나도 없었다.** 위 표의 `blog` 는 **상세**(`/blog/[slug]`) 뿐이다.
  → 둘 다 §8-5 대로 신설했다.

### 앱 (chao-vn-app)
콘솔의 위치 **1개가 앱의 여러 자리로 퍼진다** (`services/FirebaseAdService.js` 의 `POSITION_TO_SLOTS`):

| 콘솔 위치 | 앱 실제 자리 |
|---|---|
| `head` | `home_banner` + `header` + `detail_top` |
| `inner` | `home_inline` + `inline` + `detail_middle` |
| `bottom` | `fixed_bottom` + `detail_bottom` |
| `popup` | `popup` |

- 상세 3자리는 `DetailAdBanner` 컴포넌트가 담당하며 **당근·구인·부동산 상세에만** 붙어 있다.
- **뉴스 상세(`PostDetailScreen`)에는 팝업·하단고정만 있고 헤더·인너·하단이 없다.** ← 추가 대상
- 컴포넌트가 이미 있으므로 **네이티브 변경 없음 → OTA 로 배포 가능**.

---

## 2. 등록 콘솔이 보여주는 것 (`daily-news-final/app/admin/ad-center/page.js`)

- 지면: `app` · `vnkorlife` · `chaovietnam`
- 위치(`PLACEMENTS`): 앱 = head/inner/bottom/popup · 웹 2종 = top/in-content/bottom/sidebar
- 페이지(`SURFACE_PAGES`):
  - `APP_PAGES` 10종
  - `VNKORLIFE_PAGES` 8종 — **동네업소 2종 누락**
  - `CHAOVIETNAM_PAGES` 3종 (home / news-terminal / detail)

---

## 3. 확인된 불일치 5가지

1. **chaovietnam 홈** — 콘솔은 위치 4개를 고르게 하지만 실제 슬롯은 `sidebar` 하나. 나머지로 등록하면 **아무 데도 안 뜬다.**
2. **뉴스터미널** — 실제는 `in-content` 하나. `top`·`bottom`·`sidebar` 로 등록하면 안 나온다.
3. **사이드바가 페이지를 구분하지 않는다** ⚠️ — 숏코드가 `[xinchao_ad slot="sidebar" max="300"]` 로 **`page` 없이** 호출된다.
   → "상세페이지 전용"으로 판 광고가 홈 사이드바에도 뜬다. 광고주와의 약속이 지켜지지 않는다.
4. **vnkorlife 콘솔에 `neighborbusiness`·`neighborbusiness-detail` 이 없다** — 실제로는 슬롯이 4개씩 있는데 타겟 지정을 못 한다.
5. **상세페이지 `in-content` 가 실제 6칸** — 콘솔에는 1개로 보여, 한 페이지에 광고가 최대 몇 개 들어가는지 판매자가 알 수 없다.

**현재 등록된 광고 2건**(신한은행 `in-content`, 알리 `bottom`)은 `targetPages` 가 비어 "모든 페이지"인데 **실제로는 상세페이지에서만** 노출 중이다.

---

## 4. 사장님이 제시한 목표 배치 (2026-08-23)

### chaovietnam
- **시작 페이지**: 헤더 · 헤더 하단 · 사이드바(sticky, 현재의 2배 길이 = **6개**) · **각 섹션별 하단 12개**
- **상세 페이지**: 본문 상단 1 · 중간 **2** · 하단 1 (헤더·사이드바는 시작 페이지와 동일)

### 뉴스터미널
- 가장 많은 독자가 오는 페이지이므로 광고를 잘 구성해야 함
- 헤더 아래 · **각 섹션 12개 아래**

### vnkorlife.com
- **홈**: 헤더 검색창 아래 · 교민생활 서비스 제휴 박스라인 아래
- **목록**(당근·구인구직·부동산): 헤더(검색창 아래) · 사이드바 · **목록 6개당 1개** · 하단
- **상세**: 헤더 · 하단
- **옐로페이지**: 헤더 · 인너 2개 → **상세**: 헤더 · 하단
- **블로그**: 헤더 · 목록 4개당 1개 · 하단 → **상세**: 헤더 · 인너 3개 · 하단

### 앱
- 현재 구성 유지 · **뉴스 상세에 헤더·인너·하단 추가**

---

## 5. 이에 대한 의견 (2026-08-23)

**전반적으로 좋은 방향.** 다만 4가지를 짚는다.

### ⚠️ (1) chaovietnam 헤더·헤더하단은 광고차단에 걸린다 — 가장 중요
Sahifa 테마의 헤더 광고 영역은 **`e3lan`**(아랍어 '광고') 클래스로 감싸이고, 이 클래스는 광고차단 필터 목록에 등재돼 있다.
2026-08-22 쇼핑 캐러셀이 홈에서만 안 보이고 뉴스터미널에서는 보였던 것이 이 때문이었다(실측 확인).

→ **테마 광고 칸에 유료 광고를 걸면 광고차단 쓰는 독자(한국 기준 20~30%)에게 안 보인다.** 노출수가 실제보다 부풀려진다.
→ 헤더·헤더하단은 **테마 광고 영역이 아니라 우리 플러그인이 직접 심는 자체 슬롯**으로 만든다.
   클래스명에 `ad` · `banner` · `e3lan` 같은 단어를 쓰지 않는다.

### (2) 홈 20자리는 팔 물량이 받쳐줘야 의미가 있다
빈 슬롯은 자동으로 숨겨지므로 **해는 없다.** 다만 지금 등록 광고가 2건인데 자리가 20개면 대부분 빈 채로 돈다.
→ **제안**: 채울 광고가 없을 때 자체 홍보(앱 설치 · 매거진 구독 · 광고문의)를 돌리는 **"기본 광고"** 개념.
   빈 지면이 안 생기고, 광고주에게 "이 자리는 항상 노출된다"고 말할 수 있다. ← **결정 대기 항목**

### (3) 상세페이지 중간 6 → 2 는 잘한 결정
지금은 매 2단락마다 들어가 최대 6칸이다. 읽기 흐름이 끊기고, 8/21 알리 상품이 문단마다 도배된 사고도 이 구조 탓이었다.

### (4) 앱 뉴스 상세는 지금 구조로 바로 된다
`DetailAdBanner`(상단·중간·하단) 컴포넌트가 이미 있고 다른 상세 화면에서 쓰고 있다.
`PostDetailScreen` 에 3줄 추가하면 되며 **네이티브 변경이 없어 OTA 로 배포 가능**하다.
앱 슬롯에 **모자란 것은 없다.** 다만 `head` 하나가 세 자리(홈배너·리스트상단·상세상단)에 동시에 들어가므로,
특정 자리만 팔려면 **페이지 타겟팅과 함께** 써야 한다.

---

## 6. 확정 후 작업 순서

1. **슬롯 표준 확정** — 지면 × 페이지 × 위치를 이 문서에 표로 고정
2. **표시 쪽 구현** — WP 플러그인 · jenny · vnkorlife 에 슬롯 신설/정리 (광고차단 회피 클래스로)
3. **등록 콘솔 재구성** — `ad-center/page.js` 의 선택지를 실물과 1:1로. 없는 자리는 빼고, 동네업소 추가
4. **앱 OTA** — 뉴스 상세에 3자리 추가 (`eas update --channel production`)
5. **검증** — 각 페이지를 헤드리스 Chrome 으로 렌더해 슬롯이 실제로 잡히는지 확인
   (`chrome --headless=new --virtual-time-budget=20000 --dump-dom <url>`)

---

## 7. 결정 완료 (2026-08-23)

> **Q. 빈 슬롯에 자체 홍보(앱 설치 · 매거진 구독 · 광고문의)를 채울까, 그냥 비워둘까?**
> **A. 자체 홍보로 채운다.** → 구현 규격은 §9.

> **추가 결정 (사장님, 작업 중)**: **뉴스 터미널을 chaovietnam 의 '페이지'가 아니라 독립 지면으로 분리한다.**
> 왜: 독자가 가장 많이 오는 화면이고 자리 구성도 다르다(사이드바 없음 · 섹션마다 1칸).
> chaovietnam 밑의 페이지 하나로 두면 **그 지면만 따로 팔 수가 없다.** → 등록 폼의 지면이 3개 → **4개**.

---

## 8. 슬롯 표준 (정본) — 지면 × 페이지 × 위치

**이 표가 정본이다.** 등록 콘솔(`ad-center`)의 선택지도, 표시 쪽 코드도 여기에 맞춘다.
표에 없는 조합은 **콘솔에서 고를 수 없다**(팔 수 없는 자리를 파는 일을 없앤다).

### 8-1. 위치(position) 어휘 — 전 지면 공통

지금까지 지면마다 다른 낱말을 썼다(`head` vs `top`, `inner` vs `in-content`). 하나로 통일한다.

| position | 뜻 | 칸 수 |
|---|---|---|
| `header` | **사이트 헤더 바로 아래** — 페이지 최상단. 콘텐츠보다 위 | 1 |
| `top` | **콘텐츠 시작 지점** — 홈이면 '헤더 하단', 상세면 '본문 상단' | 1 |
| `in-content` | **본문·목록 중간** 반복 슬롯. 페이지마다 칸 수가 다르다 | N |
| `section` | **섹션(카테고리 블록) 하단** 반복 슬롯 | N |
| `bottom` | **페이지 끝** | 1 |
| `sidebar` | 사이드바 세로 스택 | N |
| `popup` | 앱 전면 팝업 (앱 전용) | 1 |

> **왜 `header` 와 `top` 을 나누는가**: 사장님 요구가 "헤더 · 헤더 하단" 두 칸이다. 한 낱말로는 둘을 못 판다.
> `header` 는 **어느 페이지에서나 같은 자리**(사이트 상단)라 "전 지면 노출" 상품으로 팔기 좋고,
> `top` 은 그 페이지 콘텐츠의 첫 칸이라 **페이지별로 팔린다.**

### 8-2. 지면 4개

| 지면 (`surface`) | 무엇 | 표시하는 코드 |
|---|---|---|
| `app` | 씬짜오 앱 | `chao-vn-app` |
| `vnkorlife` | vnkorlife.com | `vnkorlife-web` |
| `chaovietnam` | chaovietnam.co.kr **홈 · 기사 상세** | `daily-news-final/wordpress-plugin/xinchao-unified-ads.php` |
| `news-terminal` 🆕 | **뉴스 터미널** (독립 지면) | `daily-news-final/wordpress-plugin/jenny-daily-news.php` |

### 8-3. chaovietnam.co.kr (홈 · 기사 상세)

| 페이지 (`page`) | `header` | `top` | `in-content` | `section` | `bottom` | `sidebar` |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| `home` 시작 페이지 | 1 | 1 | — | **12** | 1 | **6** |
| `detail` 기사 상세 | 1 | 1 | **2** | — | 1 | **6** |

- **상세 중간 6 → 2**: 매 2단락마다 넣던 것을 **본문을 3등분해 2칸**으로 바꿨다.
  단락 수와 무관하게 항상 2칸이라 광고주에게 "이 기사엔 몇 칸"이라고 말할 수 있다.
- **`section` 12칸**: 홈의 카테고리 블록(`section.cat-box`) 아래마다 1칸. 라이브 홈에 **정확히 12개** 있음을 실측 확인.
- ⚠️ **`header`·`top`·`section`·`bottom` 은 테마(Sahifa) 광고 칸을 쓰지 않는다.** `.e3lan` 은 광고차단 필터에 등재돼 있다(§5-1).
  우리 플러그인이 **푸터에서 만들어 완성된 DOM 의 정해진 자리로 옮긴다.** 컨테이너 클래스는 `xc-slot`(← `ad`·`banner`·`e3lan` 금지).
- ⚠️ **사이드바가 이제 페이지를 지킨다.** `page` 를 안 넘기면 플러그인이 현재 페이지를 스스로 판정한다
  (`xinchao_current_page_bucket()`). "상세 전용"으로 판 광고가 홈에 뜨던 문제(§3-3) 해결.

### 8-4. 뉴스 터미널 (독립 지면)

화면이 **하나뿐**이라 '노출 페이지'를 고를 것이 없다 — 콘솔에서 그 칸을 숨긴다.

| `header` | `top` | `section` | `bottom` | `sidebar` |
|:--:|:--:|:--:|:--:|:--:|
| 1 | 1 | **섹션마다 1** | 1 | — |

- **사이드바가 없다.** 이 화면은 jenny 플러그인이 통째로 그리며 본문이 전체 폭을 쓴다.
  테마 헤더(`#theme-header`)도 `#main-content` 도 없는 단독 템플릿이라, 슬롯을 **서버에서 직접** 심는다.
- **섹션 수는 그날그날 다르다.** 재고가 2건 미만인 섹션은 아예 안 그려서, 2026-08-23 실측 기준 **5개**였다(최대 12).
  광고가 없는 칸은 자동으로 숨겨지므로 문제되지 않는다.

### 8-5. vnkorlife.com

| 페이지 (`page`) | 라우트 | `top` | `in-content` | `bottom` | `sidebar` |
|---|---|:--:|:--:|:--:|:--:|
| `home` 홈 | `/` | 1 ✅ | — | 1 ✅ | — |
| `market` 당근 목록 | `/market` | 1 ✅ | 6개당 1 ✅ | 1 ✅ | 1 ✅ |
| `market-detail` 당근 상세 | `/market/[id]` | 1 ✅ | 1 ✅ | 1 ✅ | 1 ✅ |
| `jobs` 구인 목록 | `/jobs` | 1 ✅ | 6개당 1 ✅ | 1 ✅ | 1 ✅ |
| `jobs-detail` 구인 상세 | `/jobs/[id]` | 1 ✅ | 1 ✅ | 1 ✅ | 1 ✅ |
| `realestate` 부동산 목록 | `/realestate` | 1 ✅ | 6개당 1 ✅ | 1 ✅ | 1 ✅ |
| `realestate-detail` 부동산 상세 | `/realestate/[id]` | 1 ✅ | 1 ✅ | 1 ✅ | 1 ✅ |
| `neighborbusiness` 동네업소 목록 | `/neighborbusiness` | 1 ✅ | 6개당 1 ✅ | 1 ✅ | 1 ✅ |
| `neighborbusiness-detail` 동네업소 상세 | `/neighborbusiness/[id]` | 1 ✅ | 1 ✅ | 1 ✅ | 1 ✅ |
| `yellowpage` 옐로페이지 | `/yellowpage` | **1 🆕** | **2 🆕** | — | — |
| `yellowpage-detail` 업소 상세 | `/biz/[id]` | **1 🆕** | — | **1 🆕** | — |
| `blog` 블로그 목록 | `/blog` | **1 🆕** | **4개당 1 🆕** | **1 🆕** | — |
| `blog-detail` 블로그 상세 | `/blog/[slug]` | **1 🆕** | **3 🆕** | **1 🆕** | 1 ✅ |

✅ = 이미 있음 · 🆕 = 이번에 신설

- **vnkorlife 에는 `header` 를 두지 않는다.** 사장님이 말한 "헤더(검색창 아래)"는 이 사이트에서 **콘텐츠 첫 칸**이고,
  그 자리를 이미 `top` 이 차지하고 있다. 낱말만 다르고 같은 자리다 — 새로 만들면 중복 슬롯이 된다.
- **상세 4종의 `in-content`·`sidebar` 는 목표(헤더·하단)보다 많지만 그대로 둔다.**
  빈 슬롯은 자동으로 숨겨져 해가 없고, 지우면 팔 수 있는 재고만 줄어든다. (⚠️ 사장님 판단으로 되돌릴 수 있음)
- ⚠️ **`blog` 값의 뜻이 바뀌었다**: 이제 **`blog`=목록 / `blog-detail`=상세**. 예전엔 상세가 `blog` 를 썼다.
  현재 등록된 광고 2건은 `targetPages` 가 비어 있어(=전체) **영향을 받지 않고**, 구 데이터용 호환 처리도 넣어 뒀다.

### 8-6. 앱 (chao-vn-app)

앱은 콘솔 위치 1개가 여러 자리로 퍼지는 구조를 **그대로 둔다**(`POSITION_TO_SLOTS`).

| 콘솔 위치 | 표준 어휘로는 | 앱 실제 자리 |
|---|---|---|
| `head` | `header` | `home_banner` · `header` · `detail_top` |
| `inner` | `in-content` | `home_inline` · `inline` · `detail_middle` |
| `bottom` | `bottom` | `fixed_bottom` · `detail_bottom` |
| `popup` | `popup` | `popup` |

| 화면 | 상단 | 중간 | 하단 | 팝업 |
|---|:--:|:--:|:--:|:--:|
| 홈 · 목록 4종 · 상세 3종(당근·구인·부동산) | ✅ | ✅ | ✅ | ✅ |
| **뉴스 상세 (`PostDetailScreen`)** | **🆕** | **🆕** | **🆕** | ✅ |

- 뉴스 상세 = `screen="news"`. 콘솔의 `magazine`·`magazine-detail` 타겟이 여기로 온다(`TARGETPAGE_TO_SCREENS`).
- **네이티브 변경 없음 → OTA 로 배포**(`eas update --channel production`). ⛔ `--branch main` / `--channel main` 금지.

---

## 9. 자체 홍보 폴백 ("기본 광고") — §7 결정에 따른 규격

**규칙**: 슬롯에 배정된 유료 광고가 없을 때만 자체 홍보를 그린다. 유료 광고가 들어오면 자동으로 밀려난다.

- **어디에 두나**: 통합센터에 광고로 등록하지 **않는다.** 등록하면 노출 통계가 유료 광고와 섞이고 만료일 관리 대상이 된다.
  → 각 표시단(WP 플러그인 · 앱 `AdBanner`)에 **폴백으로 내장**한다.
- **소재**: 이미지가 아니라 **HTML/글자 배너**다. 새 이미지 파일을 만들어 올릴 필요가 없고 어떤 폭에서도 안 깨진다.
  - 웹: 앱 설치 · 교민 생활정보 · 광고 문의
  - 앱: 매거진 · 광고 문의 (**'앱 설치'는 넣지 않는다** — 이미 앱을 쓰는 사람에게 무의미)
- **한 지면 최대 2칸.** 20칸이 전부 우리 배너면 광고가 아니라 도배로 보인다.
  - 웹: `window.__xcHouseN` 으로 페이지당 셈. **섹션 슬롯은 폴백을 끈다**(섹션이 여러 개라 켜면 줄줄이 붙는다).
  - 앱: 상세 **상단·하단**만. **중간은 비면 아무것도 안 그린다**(예전의 회색 "광고" 상자를 없앴다).
- **집계**: GA4 이벤트는 보내되 `promo_id` 를 `house_*` 로 구분한다 → 광고주 리포트에서 자체 홍보를 뺄 수 있다.

---

## 9-1. 슬롯 진단 모드 — 빈 자리를 눈으로 확인하는 법

**문제**: 광고가 없는 슬롯은 스스로 숨는다. 그래서 "자리가 제대로 잡혔는지"를 확인할 방법이 없었다.
2026-08-23 첫 배포 직후 홈 16칸 중 **3칸만 채워져 나머지 13칸이 어디 있는지 보이지 않았다.**

**쓰는 법** (`xinchao-unified-ads.php` v4.5.0 이상)

| 주소 | 하는 일 |
|---|---|
| `https://chaovietnam.co.kr/?xcads=debug` | 진단 모드 **켜기** |
| `https://chaovietnam.co.kr/?xcads=off` | **끄기** |

- 한 번 켜면 `sessionStorage` 에 남아 **페이지를 옮겨 다녀도 유지된다.** 브라우저 탭을 닫으면 풀린다.
- **켠 사람 브라우저에서만 보인다.** 독자에게는 아무 영향이 없다.

**보이는 것**

| 이름표 | 뜻 |
|---|---|
| 🟢 `광고 · <지면> / <자리> #n` | 유료 광고가 채워진 칸 |
| 🟣 `자체 홍보 · …` | 폴백이 채운 칸 (§9) |
| ⬜ `빈 슬롯 · …` (점선 상자) | 자리는 있는데 채울 것이 없는 칸 |
| 🔴 화면 왼쪽아래 `⚠ 자리를 못 찾은 슬롯: …` | 테마 DOM 에서 꽂을 자리를 못 찾은 칸 |

마지막 빨간 경고가 뜨면 **테마가 바뀌어 기준점(`#theme-header` · `#main-content .content` · `section.cat-box`)이 사라진 것**이다.
그 경우에도 페이지는 안 깨지고 그 칸만 없는 것이 된다.

**2026-08-23 라이브 실측** (헤드리스 Chrome 렌더 후 DOM 확인 — 슬롯이 실제로 잡히는지만 본 것)

| 페이지 | 슬롯 | 채워짐 | 빈칸 |
|---|--:|---|--:|
| 홈 | 16 | 자체홍보 2(헤더·사이드바) + 유료 1(하단) | 13 |
| 뉴스 터미널 | 14 | 자체홍보 2(헤더·상단) | 12 |
| 기사 상세 | 6 | 유료 2 + 자체홍보 2 | 2 |

→ **설계대로 동작하고 있다.** 빈칸이 많은 이유는 두 가지다:
① 자체 홍보는 **페이지당 2칸**이 상한(`XINCHAO_HOUSE_MAX`)이고, ② `section` 슬롯은 폴백을 꺼 뒀다(줄줄이 붙는 것 방지).
자체 홍보를 더 채우고 싶으면 플러그인의 `XINCHAO_HOUSE_MAX` 숫자만 올리면 된다.

---

## 9-2. 옛 광고 자리 전수 조사 — 이중 슬롯 정리 (2026-08-23)

새 슬롯을 넣기 전부터 있던 광고 자리들과 겹치는지 확인한 결과다.
**헤드리스 Chrome 으로 렌더한 DOM 을 문서 순서대로 훑어** 무엇이 어디에 있고 안에 뭐가 들었는지 직접 봤다.

| 종류 | 어디 | 안에 든 것 | 판정 |
|---|---|---|---|
| 🗑 **옛 `jenny-ad-section` 13개** | 뉴스터미널 | **전부 비어있음** | 🔴 **죽은 이중 → 제거함** |
| 🎨 `e3lan300_600-widget` ×2 | 홈·상세 **사이드바** | **신풍제약 · CIS 국제학교** | 🟢 **살아있는 유일한 e3lan** |
| 🎨 `e3lan-top` | 사이트 헤더 **안** | 비어있음 | ⬜ 빈 칸 |
| 🎨 `e3lan-below_header` | 헤더 바로 아래 | 비어있음 | ⬜ 빈 칸 |
| 🎨 `home-e3lan` | 홈 본문 칼럼 | 비어있음 | ⬜ 빈 칸 |
| 🎨 `e3lan-post` ×2 | 기사 본문 칼럼 | 비어있음 | ⬜ 빈 칸 |
| 🎨 `e3lan-bottom` | 페이지 맨 아래 | 비어있음 | ⬜ 빈 칸 |
| 🤖 애드센스 자동광고 | 전 페이지 | 살아있음(홈 10곳) | 🟢 우리 슬롯과 무관하게 알아서 끼어든다 |

> ⚠️ **뉴스 터미널에는 e3lan 이 하나도 없다** — 그 화면은 테마 템플릿을 쓰지 않는다.

> 🔧 **조사 방법 주의 — 앞선 조사 한 번을 이렇게 틀렸다.**
> 처음엔 여는 태그 뒤 3,000자를 통째로 읽어 내용을 판정했다. 그러면 **옆 요소 내용까지 끌어와**
> 빈 `e3lan-top` 을 "애드센스 있음"으로 잘못 읽는다. 실제로 그렇게 잘못 보고했다.
> → **DOM 조상 체인을 추적하는 진짜 HTML 파서**로 다시 봐야 한다(`html.parser` 로 여는/닫는 태그를 세어
> 자기 범위 안만 읽는다). 중첩된 `.e3lan-cell` 을 별개 칸으로 세던 문제도 같이 해결된다.

### 제거한 것 — 옛 `jenny-ad-section` 13개 (jenny v2.5.0)

`jenny-ad-top` · `jenny-ad-after-topnews` · `jenny-ad-<섹션>-end` ×11.

- **Ad Inserter 플러그인이 채우라고 만든 자리인데, 그 플러그인이 이미 없다.** 렌더된 DOM 에서 13개 전부
  `<ins>` 도 `<iframe>` 도 없이 비어 있었다(`Ad Inserter 흔적 0건`).
- 그런데 CSS 가 `min-height: 100~200px` 짜리 **회색 점선 상자**를 그려서, 한 페이지에 **빈 공간이 1,500px 넘게** 생겼다.
  화면에 보이던 애드센스는 **자동광고**가 그 근처에 따로 넣은 것이라 이 칸들과 아무 관계가 없었다.
- 같은 자리를 통합센터 슬롯이 이미 1:1 로 맡고 있었다(각 옛 칸 **바로 아래**에 새 슬롯이 붙어 있었다).
- '주요 뉴스 뒤' 자리만 대응 슬롯이 없었으므로 **`section` 0번으로 살렸다** — 자리를 잃지 않는다.
  섹션별 슬롯은 1번부터 매겨진다.
- 죽은 CSS(`.jenny-ad-placeholder` 등)도 함께 지웠다. **새 슬롯 감싸개(`.jenny-slot-*`)에는 높이를 주지 않는다** —
  높이를 주면 광고가 없을 때 또 회색 상자가 남는다. 방금 지운 것과 똑같은 문제가 된다.

### ✅ 해결됨 — 사이드바의 신풍제약 · CIS (2026-08-23)

**문제**: `.e3lan` 은 EasyList 에 **전역 차단 규칙**이 걸려 있다. 추정이 아니라 원본에서 확인했다:

```
##.e3lan                  ← 앞에 도메인이 없다 = 모든 사이트에 적용
##.e3lan-top
##.e3lan-widget-content   ← 신풍제약·CIS 가 바로 이 안에 있었다
```

사이드바 위젯 구조가 `div.widget.e3lan.e3lan300_600-widget` ▸ `div.e3lan-widget-content` ▸ 이미지라
**바깥·안쪽 두 겹 모두** 걸렸다 → 돈 받는 광고 2건이 광고차단 쓰는 독자(한국 기준 20~30%)에게 **안 보였다.**

**해결의 실마리는 같은 사이드바 안에 있었다**: 신한은행·Vietjet 은 **일반 이미지 위젯**(`widget_media_image`)이고
잘 돌고 있었다. EasyList 의 `widget_media_image` 규칙은 **전부 도메인 지정**이고 chaovietnam 은 그 목록에 없다.
→ **통합센터로 옮길 것도 없이, 위젯 종류만 바꾸면 된다.**

**한 일** (워드프레스 REST API 로 직접 실행)

| 자리 | 전 | 후 |
|---|---|---|
| 7번 | `Sahifa - Ads 300*600: 신풍제약` | `이미지: 신풍제약` (`media_image-48`) |
| 9번 | `Sahifa - Ads 300*600: CIS` | `이미지: CIS(캐나다 국제학교)` (`media_image-49`) |

- **자리·이미지·링크 전부 그대로.** 검증: 신풍제약 → `shinpoong.com.vn`, CIS → `ssv.cis.edu.vn` 링크 보존 확인
- 옛 위젯 2개는 **지우지 않고 '비활성 위젯'으로 옮겼다** → 되돌릴 수 있다
- 라이브 확인: 사이드바에 `e3lan-widget-content` **0개**, 광고 4건 모두 정상 표시

**남은 e3lan 4곳**(`top`·`below_header`·`home-e3lan`·`bottom`)은 **전부 비어 있고 사장님 결정으로 그대로 둔다.**
⚠️ 나중에 테마 옵션으로 여기 뭔가 넣으면 우리 슬롯과 광고 두 칸이 나란히 붙는다는 것만 기억할 것.

**운영 규칙 (2026-08-23 확정)**
1. 광고 표시는 **통합 광고센터**가 담당한다
2. **사이드바만 워드프레스 위젯으로 관리한다** — 단, `Sahifa - Ads` 위젯이 아니라 **일반 「이미지」 위젯**을 쓴다
3. 한 소재는 한 곳에만 — 통합센터에 등록했으면 위젯에서 뺀다 (같은 광고가 두 번 뜨는 것 방지)

---

## 9-2-1. chaovietnam 사이드바 — 통합센터 슬롯 창구 (2026-08-23)

**핵심**: 통합 광고센터는 **데이터만** 갖고 있다. 화면에 내보내는 것은
**워드프레스 텍스트 위젯 안의 숏코드**다. 창구가 없으면 아무리 등록해도 안 뜬다.

### 왜 창구를 여러 개 두는가

창구가 하나면 그 위치의 광고가 **전부 그 한 칸에 세로로 쌓인다.**
그러면 검색·댓글 위젯 사이에 광고를 끼워 넣은 지금 배치가 깨지고, 광고 4개가 연달아 붙는다.
→ **자리마다 창구를 하나씩** 두고 `n` 으로 순번을 지정한다.

### 현재 구성 (Primary Widget Area, 12칸)

| # | 위젯 | 내용 |
|---|---|---|
| 1 | 🔍 검색 | 비광고 |
| 2 | 🖼 이미지 | 이번호/지난호 기사 |
| 3 | **📝 `text-2`** | **`[xinchao_ad slot="sidebar" n="0" max="300" house="0"]`** |
| 4 | 💬 최근 댓글 | 비광고 |
| 5 | 🖼 이미지 | 광고문의 (자체 홍보) |
| 6 | 🖼 이미지 | 신한은행 |
| 7 | **📝 `text-3`** | **`… n="1" …`** |
| 8 | 🖼 이미지 | 신풍제약 |
| 9 | 🖼 이미지 | Vietjet |
| 10 | **📝 `text-4`** | **`… n="2" …`** |
| 11 | 🖼 이미지 | CIS(캐나다 국제학교) |
| 12 | **📝 `text-5`** | **`… n="3" …`** |

### 숏코드 읽는 법

| 값 | 뜻 |
|---|---|
| `slot="sidebar"` | 통합센터에서 **위치=사이드바**로 등록한 광고만 |
| `n="0"` | 그 위치 광고 중 **우선순위 1등**. `n="1"` = 2등 … |
| `max="300"` | 최대 폭(px). 사이드바 폭 |
| `house="0"` | **자체 홍보 폴백 끔** |
| `page` 없음 | 지금 보는 페이지를 **스스로 판정** — 홈/상세를 가려 낸다 |

### ⚠️ 자리는 고정이 아니다 — 우선순위로 정해진다

`n` 은 "이 위젯엔 이 광고"가 아니라 **"우선순위 몇 번째 광고"**다.

> 광고 4건(우선순위 1·2·3·4)이 n=0,1,2,3 에 들어가 있을 때 **2번이 만료되면 3·4번이 한 칸씩 위로 당겨진다.**

→ 광고주에게 "사이드바 맨 위"를 팔았다면 **우선순위 숫자로 관리**한다. 우선순위 1 = 항상 첫 창구.

### 왜 사이드바만 `house="0"` 인가

1. 사이드바엔 이미 자체 홍보 이미지 위젯이 둘 있다(지난호 · 광고문의) → **중복**
2. 자체 홍보는 **한 페이지 2칸**이 한도인데, 사이드바 스크립트가 푸터보다 **먼저** 돌아
   그 2칸을 먼저 가져가면 **헤더·상단 같은 눈에 띄는 자리가 빈다**(실측으로 확인)

→ 사이드바에서 끄니 헤더·상단이 자체 홍보를 받고, 하단은 유료(알리)가 채웠다. 의도한 배분이다.

### 같이 고친 것 (라이브에서 발견)

- `text-2` 의 **제목이 "테스트"** 였다 → 사이드바에 그 글자가 그대로 보이고 있었다. 제목을 비웠다.
- 숏코드가 **`<strong><code>` 로 감싸여** 있었다 → 광고가 코드 블록 안에서 렌더됐다. 순수 숏코드로 바꿨다.
  (`visual: false` 로 두어 시각 편집기가 다시 감싸지 못하게 했다)

### 앞으로 사이드바 광고를 넣는 법

1. 통합 광고센터 → 지면 **chaovietnam** · 위치 **사이드바** · 이미지·링크·기간·**우선순위**
2. 저장하면 **1분 안에** 자동으로 뜬다 — 워드프레스는 건드릴 필요 없다
3. 창구가 4개이므로 **동시에 4건**까지. 더 필요하면 텍스트 위젯을 늘리고 `n="4"`, `n="5"` …
4. 옛 이미지 위젯에 있던 광고를 통합센터로 옮겼다면 **그 이미지 위젯을 반드시 지운다**(두 번 뜨는 것 방지)

---

## 9-3. 🚨 플러그인 업로드 사고 (2026-08-23) — 다시는 이렇게 잃지 말 것

**증상**: 광고가 전부 사라지고(홈 16칸 → 0), 쇼핑 캐러셀도 사라지고,
**모든 페이지가 `<!DOCTYPE>` 앞에 정체불명의 250바이트를 뱉었다.**

**원인**: FTP 업로드 중 `wp-content/plugins/xinchao-unified-ads.php` 의 **내용이 통째로 덮어써졌다.**
PHP 코드 대신 이런 텍스트 한 줄만 남았다 — 셸이 `wordpress-plugin/*` 를 펼친 결과물이다:

```
wordpress-plugin/mu-plugins wordpress-plugin/jenny-daily-news.php wordpress-plugin/XINCHAO-...
```

**왜 그 증상이 났는가 (메커니즘)**

| 증상 | 이유 |
|---|---|
| 모든 응답 앞에 쓰레기 | 워드프레스 `active_plugins` 에 이 파일이 **활성**으로 남아 있어 매 요청 `include` 된다. `<?php` 가 없으니 내용이 **그대로 출력**된다 |
| 플러그인 목록에서 사라짐 | 목록은 `Plugin Name:` 헤더를 읽어 만든다. 헤더가 없으니 인식 안 됨 |
| 광고·쇼핑 캐러셀 전멸 | `xinchao_render_ad()` 가 정의되지 않아 jenny 와 숏코드가 전부 건너뜀 |
| robots.txt · RSS · REST 오염 | PHP 를 거치는 모든 출력이 영향. **자동 기사 발행이 REST 를 쓴다** |

**해결**: 올바른 파일을 다시 올리는 것만으로 끝. 활성 목록에 이미 있어 별도 활성화 불필요.

### ✅ 업로드 후 30초 점검법 (앞으로 반드시)

```
https://chaovietnam.co.kr/wp-content/plugins/xinchao-unified-ads.php
```

- **빈 화면** → 정상 (플러그인은 `if (!defined('ABSPATH')) exit;` 로 조용히 끝난다)
- **글자가 보임** → 업로드가 깨진 것

⚠️ **워드프레스 관리화면의 플러그인 목록도 함께 본다.** 판번호가 안 맞거나 항목이 사라졌으면 그 자체가 신호다.
이번 사고에서 **목록에서 사라진 것**이 결정적 단서였다.

---

---

## 10. 진행 현황

| # | 작업 | 상태 |
|---|---|---|
| 1 | 슬롯 표준 확정 (§8) | ✅ 2026-08-23 |
| 2 | 표시 쪽 구현 — WP · jenny · vnkorlife | ✅ 코드 완료 (**WP 는 FTP 업로드 대기**) |
| 3 | 등록 콘솔 재구성 + 뉴스터미널 지면 분리 | ✅ |
| 4 | 앱 — 뉴스 상세 3자리 | ✅ 코드 완료 · **OTA 미발송** |
| 5 | 검증 | 🟡 정적 검증만 끝 — 라이브 확인은 배포 후 |
| 6 | 자체 홍보 폴백 (§9) | ✅ 웹·앱 모두 |

**정적 검증으로 확인한 것**
- `vnkorlife-web` — `tsc --noEmit` 통과 · `next build` 통과
- `daily-news-final` — `next build` 통과
- `chao-vn-app` · WP 플러그인 — 파서/괄호 균형 검사 통과 (PHP CLI 가 없어 `php -l` 은 못 돌림)
- 라이브 HTML 실측 — chaovietnam 홈에 `section.cat-box` **12개**, `#theme-header`·`#main-content .content` 존재 확인.
  뉴스터미널은 그 셋이 **하나도 없는** 단독 템플릿임을 확인(그래서 서버 삽입).

---

## 11. 무엇을 고쳤는지 (2026-08-23 구현)

### `vnkorlife-web` — 배포: git push → Vercel 자동
| 파일 | 바꾼 것 |
|---|---|
| `src/lib/models/ads.ts` | 페이지 값 추가: `yellowpage` · `yellowpage-detail` · `blog-detail` |
| `src/components/detail/AdBanner.tsx` | 구 데이터 호환 — 예전에 `blog` 로 등록한 광고가 `blog-detail` 에서도 잡히게 |
| `app/yellowpage/YellowPageClient.tsx` | 🆕 상단 1 + 목록 사이 2칸 |
| `app/biz/[id]/BizDetailClient.tsx` | 🆕 상단 1 + 하단 1 |
| `app/blog/page.tsx` | 🆕 상단 1 + 목록 4개당 1 + 하단 1 |
| `app/blog/[slug]/page.tsx` | 🆕 상단 1 + 본문 3칸 + 하단 1 · 페이지값 `blog`→`blog-detail` |

본문 3칸은 **애드센스가 쓰는 경계를 피해서** 놓는다 — 안 그러면 광고 둘이 붙어 나온다.

### `daily-news-final` — 콘솔·API 는 git push → Vercel / **WP 플러그인은 FTP 수동**
| 파일 | 바꾼 것 |
|---|---|
| `wordpress-plugin/xinchao-unified-ads.php` (v4.6.0) | ① 컨테이너 클래스 `xinchao-ad` → **`xc-slot`**(광고차단 회피) ② 현재 페이지 자동판정 — 사이드바가 더 이상 페이지를 섞지 않는다 ③ 본문 중간 **매 2단락(최대6) → 본문 3등분 2칸** ④ 홈·상세에 `header`·`top`·`section`·`bottom` 슬롯을 **JS 로 테마 DOM 에 꽂는다** ⑤ 자체 홍보 폴백 |
| `wordpress-plugin/jenny-daily-news.php` (v2.5.0) | 뉴스터미널에 `header` 1 · `top` 1 · `section` 섹션당 1 · `bottom` 1. 예전 `in-content` 호출을 `top` 으로 고침 |
| `app/admin/ad-center/page.js` | 지면 **3개 → 4개**(뉴스터미널 분리) · 위치에 `header`·`section` 추가 · 페이지에 동네업소·옐로페이지·블로그상세 추가 · **`PAGE_SLOTS`** 로 없는 조합은 못 고르게 |
| `app/api/public/ads/route.js` | `news-terminal` 지면 지원 (`array-contains-any`) + 구 chaovietnam 타겟팅 호환 |

⚠️ **뉴스터미널의 `in-content` → `top` 변경은 지금 떠 있는 광고에 영향이 있다.**
현재 신한은행이 `in-content` 로 등록돼 있어, 플러그인을 올리면 터미널에서는 빠지고 **기사 상세에만** 남는다.
→ 터미널에도 띄우려면 콘솔에서 **지면 "뉴스 터미널"을 체크하고 위치를 고른 뒤** 다시 저장한다.

### `chao-vn-app` — 배포: git push → **`eas update --channel production`**
| 파일 | 바꾼 것 |
|---|---|
| `screens/PostDetailScreen.js` | 뉴스 상세에 `DetailAdBanner` 상단·중간·하단 3자리 추가 |
| `components/AdBanner.js` | 빈 자리의 회색 "광고" 상자 → 자체 홍보 배너(상단·하단 2칸) · 중간은 비면 아무것도 안 그린다 |

---

## 12. 다음 단계

1. **WP 플러그인 2개를 FTP 로 올린다** (사장님) — 서버 `wp-content/plugins/`
   - `xinchao-unified-ads.php` **v4.6.0** · `jenny-daily-news.php` **v2.5.0**
   - ⚠️ 올린 뒤 워드프레스 플러그인 목록에서 **판번호가 이 값인지 눈으로 확인**한다. 올렸다고 믿지 말 것.
2. **앱 OTA** — `eas update --channel production` (미발송)
3. **라이브 검증** — 배포 후 각 페이지를 헤드리스 Chrome 으로 렌더해 `xc-slot` 이 제자리에 잡히는지 확인
   `chrome --headless=new --virtual-time-budget=20000 --dump-dom <url> | grep -c xc-slot`
   ⚠️ 자동화 플래그로는 **광고가 채워지는지**는 판단할 수 없다(§트랩 3). 슬롯 존재 확인용으로만 쓴다.
4. **등록 광고 2건 재배치** — 신한은행·알리를 새 지면·위치 체계로 다시 저장
